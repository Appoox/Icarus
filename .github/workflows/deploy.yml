name: Deploy to server

on:
  push:
    branches: master

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          clean: false

      - name: Deploy
        run: ./deploy.sh
